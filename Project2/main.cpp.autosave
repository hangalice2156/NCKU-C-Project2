#include "mainwindow.h"
#include <QApplication>

int main(int argc, char *argv[])
{
    QApplication a(argc, argv);
    MainWindow w;
    w.resize(961,641);
    w.move(480,320);
    w.setWindowTitle(QObject::tr("Prototype"));
    w.show();

    return a.exec();
}
